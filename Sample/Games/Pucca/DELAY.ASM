DELAY PROC

PUSH AX
PUSH BX
PUSH CX
PUSH DX


MOV AH,2CH
INT 21H
ADD TIME,DH

XOR AX,AX
XOR DX,DX
MOV AL,TIME
MOV BX,60
DIV BX
MOV TIME,DL

@LOOP:
MOV AH,2CH
INT 21H
CMP TIME,DH
JNE @LOOP

POP DX
POP CX
POP BX
POP AX

RET
DELAY ENDP
